"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.csvDataParser = void 0;
function csvDataParser(map, data) {
    data = data.replace(/(\r\n|\n|\r)/gm, '');
    const tiles = new Array(map.width * map.height);
    const idStrings = data.split(',');
    idStrings.forEach((idStr, index) => {
        const id = parseInt(idStr);
        if (id > 0) {
            tiles[index] = map.getTileById(id);
            if (!tiles[index]) {
                // throw new Error(`Was unable to locate tile with gid: ${id}`);
            }
        }
    });
    return tiles;
}
exports.csvDataParser = csvDataParser;
//# sourceMappingURL=csvDataParser.js.map